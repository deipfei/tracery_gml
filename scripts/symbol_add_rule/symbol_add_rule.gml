/// @param symbol
/// @param rule
///symbol_add_rule(symbol, rule))

var symbol = argument0, rule = argument1;

//I don't know what this one does, because currentRules.addRule doesn't seem to exist,
//unless currentRules is also a symbol... skipping for now

return noone;